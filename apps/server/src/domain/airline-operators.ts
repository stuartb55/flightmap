// Preserve the server-domain import path while sharing the offline reference
// with UI surfaces that also enrich callsigns.
export {
  airlineOperatorFromCallsign,
  airlineOperatorRows,
  airlineOperators,
  type AirlineOperatorMatch
} from "@flightmap/shared";
